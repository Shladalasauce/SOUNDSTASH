import { ScrollView,StyleSheet,Text,TextInput,View } from 'react-native';import{useState}from'react';import{colors}from'@/constants/theme';import{TrackRow}from'@/components/TrackRow';import{useSoundStash}from'@/store/useSoundStash';import{playQueue}from'@/services/audio/player';
export default function Library(){const[q,setQ]=useState('');const tracks=useSoundStash(s=>s.tracks);const setCurrent=useSoundStash(s=>s.setCurrent);const filtered=tracks.filter(t=>`${t.title} ${t.creatorName} ${t.platform}`.toLowerCase().includes(q.toLowerCase()));const play=async(id:string)=>{setCurrent(id);await playQueue(filtered,id)};return <ScrollView contentContainerStyle={s.page}><Text style={s.h1}>Your Library</Text><TextInput value={q} onChangeText={setQ} placeholder="Search sounds, creators, platforms" placeholderTextColor={colors.muted} style={s.search}/><View style={s.pills}><Text style={s.active}>All</Text><Text style={s.pill}>TikTok</Text><Text style={s.pill}>Instagram</Text><Text style={s.pill}>Downloaded</Text></View><Text style={s.count}>{filtered.length} SOUNDS · RECENTLY ADDED</Text>{filtered.map(t=><TrackRow key={t.id} track={t} onPress={()=>play(t.id)}/>)}</ScrollView>}
const s=StyleSheet.create({page:{padding:20,paddingTop:64,paddingBottom:160,backgroundColor:colors.background},h1:{fontSize:33,fontWeight:'900',color:colors.text},search:{backgroundColor:colors.raised,color:colors.text,padding:15,borderRadius:14,marginVertical:20},pills:{flexDirection:'row',gap:8,marginBottom:25},pill:{color:colors.muted,borderWidth:1,borderColor:colors.border,borderRadius:18,paddingHorizontal:12,paddingVertical:7,fontSize:12},active:{color:'#08090B',backgroundColor:colors.accent,borderRadius:18,paddingHorizontal:14,paddingVertical:8,fontSize:12,fontWeight:'800'},count:{color:colors.muted,fontSize:10,letterSpacing:1.4,marginBottom:8}});
