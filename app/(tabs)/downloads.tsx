import{Alert,Pressable,ScrollView,StyleSheet,Text}from'react-native';import{colors}from'@/constants/theme';import{TrackRow}from'@/components/TrackRow';import{downloadTrack}from'@/services/downloads/download';import{useSoundStash}from'@/store/useSoundStash';
export default function Downloads(){const tracks=useSoundStash(s=>s.tracks);const update=useSoundStash(s=>s.updateTrack);const downloaded=tracks.filter(t=>t.downloadState==='DOWNLOADED');const download=async()=>{const track=tracks.find(t=>t.downloadState==='NOT_DOWNLOADED');if(!track)return;update(track.id,{downloadState:'DOWNLOADING'});try{const result=await downloadTrack(track);update(track.id,{downloadState:'DOWNLOADED',localFilePath:result.uri});Alert.alert('Downloaded',`${track.title} is ready offline.`)}catch(e){update(track.id,{downloadState:'FAILED'});Alert.alert('Download failed',String(e))}};return <ScrollView contentContainerStyle={s.page}><Text style={s.h1}>Downloads</Text><Text style={s.sub}>{downloaded.length} sounds available offline</Text><Pressable style={s.button} onPress={download}><Text style={s.bt}>Download next demo sound</Text></Pressable>{downloaded.length?downloaded.map(t=><TrackRow key={t.id} track={t} onPress={()=>{}}/>):<Text style={s.empty}>Downloads are stored privately on this device. Download a demo track, then enable Airplane Mode to test offline playback.</Text>}</ScrollView>}
const s=StyleSheet.create({page:{padding:20,paddingTop:64,paddingBottom:160},h1:{fontSize:33,fontWeight:'900',color:colors.text},sub:{color:colors.muted,marginTop:7},button:{backgroundColor:colors.accent,padding:14,borderRadius:20,marginVertical:25,alignSelf:'flex-start'},bt:{fontWeight:'900'},empty:{color:colors.muted,lineHeight:22,backgroundColor:colors.surface,padding:20,borderRadius:15}});
